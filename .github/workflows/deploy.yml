name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout public repository
        uses: actions/checkout@v3

      - name: Clone private config repository
        env:
          TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
          USER: ${{ secrets.GIT_USERNAME }}
        run: |
          git clone https://${USER}:${TOKEN}@github.com/${USER}/declaration_config.git temp-config
          cp temp-config/config.js ./config.js
          ls -al

      - name: Deploy to GitHub Pages
        run: |
          git config --global user.name "${{ secrets.GIT_USERNAME }}"
          git config --global user.email "${{ secrets.GIT_USERNAME }}@users.noreply.github.com"
          git checkout --orphan gh-pages
          git add .
          git commit -m "Deploy"
          git push -f origin gh-pages
